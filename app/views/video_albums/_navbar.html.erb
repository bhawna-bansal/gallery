<nav class="navbar navbar-expand-lg bg-body-tertiary">
<div class="container-fluid">


  <%= link_to 'vidHub', root_path, class: 'navbar-brand text-decoration-none fs-2 color my-auto' %>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav mb-2 mb-lg-0">
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to 'MyAlbums', video_albums_path, class: 'nav-link text-decoration-none fs-2 color' %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to 'NewAlbum', new_video_album_path, class: 'nav-link text-decoration-none fs-2 color mx-3' %>
        <% end %>     
      </li>
    </ul>
    <form class="d-flex">
      <% if user_signed_in? %>
        <%= link_to 'Edit profile', edit_user_registration_path, class: 'btn btn-outline-secondary text-decoration-none fs-4 me-2 mx-1 my-auto' %>
        <%= link_to 'Sign Out', destroy_user_session_path, class: 'btn btn-secondary text-decoration-none fs-4 mx-2 my-auto', data: { turbo_method: :delete } %>
      <% else %>
        <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-secondary text-decoration-none mx-2 fs-4 my-auto' %>
        <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-danger text-decoration-none fs-4 my-auto' %>
      <% end %>
    </form>
  </div>
</div>
</nav>

